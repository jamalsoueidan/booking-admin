import type { Meta } from "@storybook/react";
import { CollectionEmpty } from "./collection-empty";

const meta = {
  title: "Components/Collection/CollectionEmpty",
  component: CollectionEmpty,
} satisfies Meta<typeof CollectionEmpty>;

export default meta;
