import { Meta } from "@storybook/blocks";

<Meta title="Components/Inputs/InputTags" />

A component that render select with all tags

### Component usage

```js
const field = useField("");
return <InputTags field={field} />;
```
