import { Meta } from "@storybook/blocks";

<Meta title="Components/Inputs/InputTimeZone" />

A component that generate timezone picker.

### Component usage

```js
const field = useField("timezone");
return <InputTimeZone {...field} />;
```
