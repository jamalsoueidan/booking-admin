import { Meta } from "@storybook/blocks";

<Meta title="Hooks/useDate" />

### Component usage

Use the different hook methods to convert date from tz to another.

```js
const { toUtc, toTimeZone } = useDate();
```
